import { COLORS, FONT_SIZE } from "../../constants/theme"

export const styles = {
    container: {
        paddingRight: 15,
        marginBottom: 15
    },
    icone:{
        width: 240,
        height: 140
    },
    descricao:{
        fontSize: FONT_SIZE.sm,
        color: COLORS.dark_gray,
        textAlign: "center"
    },
}
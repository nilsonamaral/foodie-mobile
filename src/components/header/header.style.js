import { COLORS, FONT_SIZE } from "../../constants/theme"

export const styles = {
    header: {
        width: "100%",
        alignItems: "center"
    },
    logo:{
        width: 170,
        height: 50,
    },
    titulo: {
        fontSize: FONT_SIZE.md,
        color: COLORS.gray,
        padding: 10,
        textAlign: "center"
    }
}
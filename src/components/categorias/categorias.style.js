import { COLORS, FONT_SIZE } from "../../constants/theme"

export const styles = {
    container: {
        paddingRight: 15,
        marginBottom: 15
    },
    icone:{
        width: 75,
        height: 75
    },
    descricao:{
        fontSize: FONT_SIZE.sm,
        color: COLORS.dark_gray,
        textAlign: "center"
    },
}